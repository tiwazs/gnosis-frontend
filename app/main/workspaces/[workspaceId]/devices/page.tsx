import WorkspacePlaceholder from "../WorkspacePlaceholder";

export default function DevicesPage() {
    return (
        <WorkspacePlaceholder
            kicker="Resource"
            title="Devices"
            body="Cameras, gates, and edge clients that belong to this workspace will be listed here."
        />
    );
}
