import WorkspacePlaceholder from "../WorkspacePlaceholder";

export default function IotControlPage() {
    return (
        <WorkspacePlaceholder
            kicker="Service"
            title="IoT Control"
            body="Connect devices registered to this workspace and send control commands from here. The live console is still being built."
        />
    );
}
